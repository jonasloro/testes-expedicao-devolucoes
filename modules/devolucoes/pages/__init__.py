"""Páginas (UI) do Centro de Tratamento de Devoluções.

Cada arquivo aqui expõe uma função `render()` (ou `render(...)` com os
parâmetros que a página precisa) chamada pelo app.py principal. A lógica de
banco/regras de negócio continua em modules/devolucoes/*.py — este pacote
cuida só da apresentação em Streamlit.
"""
